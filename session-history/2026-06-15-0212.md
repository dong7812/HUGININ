# Session [2026-06-15 02:12]

**Intent**: EXPLORING (runner-up: FEATURE_BUILDING)
**Signal**: HIGH
**Frames active**: A (0.3 pruned) / B (0.8) / C (1.0) / D (0.7)

## What shipped

- 전체 md 파일 12개 MCP-first 아키텍처 기준으로 재작성
- Git repo 초기화 + GitHub private repo 생성 (dong7812/HUGININ)
- README local dev 섹션 + server/.env.example 추가
- 제품 핵심 방향 재정립 → README, PLANS, AGENTS, DECISIONS, landing page 반영
- 랜딩페이지 hero + before/after 데모 + 비교 테이블 전면 재작성

## What was figured out

**핵심 포지셔닝 분기 (Frame C 1.0)**

세 개의 포지셔닝 후보가 명시적으로 비교됐다:

1. "CLI-First LLMOps" (이전) — 기존 README
2. "Claude Code × Team Memory" — MCP recall 루프가 핵심
3. "AI 협업 가시화 — 팀이 AI로 개발할 때 서로 뭘 하는지 보인다" ← 채택

전환 계기: "개인이 md 파일로 정리해서 공유하는 것과의 차이점"이라는 외부 피드백.
피드백이 정확히 핵심을 짚었고, 방어할 수 없었던 이유는 출발점이 언어화되지 않았기 때문.
출발점 확인 ("팀에서 프롬프트 공유가 안 돼서 병목이 생겼던 경험") 이후 방향이 잡혔다.

**Frame A/B/C/D 구현 여부 발견**

"AI: 구조 제안 | 개발자: 방향 결정 | AI: 엣지케이스 발견" 표현이 README와 랜딩페이지에 있었으나
실제 서버 코드에는 해당 로직이 전혀 없었다. collab-proof 스킬이 Claude Code 세션에서
개인 단위로 분석할 뿐, HUGININ 서버와 연결되지 않은 상태.

→ 허위 클레임 제거. Phase 1 (현재)과 Phase 2 (예정)를 명확히 분리.
→ Phase 2 구현 명세: FrameAnalysisService + Frame A/B/C/D 스코어 DB 저장 + 타임라인 배지.

**MD 파일 피드백 대응 논리**

MD 파일은 "의식적으로 쓰는 사람"이 있어야 작동한다.
HUGININ의 실질적 차별점은 자동 수집 + 결정 단위 AI 기여도 분석 — 둘 다 MD 파일에는 없다.
"저장이 목적이 아니라 가시화가 목적"이라는 원칙이 나온 지점.

## Decisions made this session

- [2026-06-15] 핵심 방향: AI 협업 가시화 + Frame A/B/C/D 팀 스케일 확장 → DECISIONS.md

## Where it got hard

- "핵심이 흔들리는 거 같아" — 피드백이 실제로 제품의 약점을 건드렸을 때의 반응
  → 해결: 출발 경험 (프롬프트 미공유 + 병목) 으로 돌아가서 재정립
- Frame A/B/C/D가 구현돼 있다고 묵시적으로 가정한 채 문서화했던 것
  → 코드 검색으로 사실 확인 후 문서 수정

## AI contribution summary

이 세션에서 Claude의 실질적 기여는 두 가지다. 첫째, README와 랜딩페이지의 허위 클레임을
코드 검색(`grep -r "frame\|Frame"`)으로 사실 확인하고 명시적으로 지적했다. 둘째,
"방향성이 흔들리지 않는 기준"을 테이블로 구조화하는 방식을 제안했다 — 각 경쟁사 비교 질문에
대한 답을 미리 정의해두는 포맷. 포지셔닝 선택 자체는 개발자가 "실제 출발 경험"을 명확히
했을 때 자연스럽게 수렴됐고, Claude는 그 결정을 문서로 구현하는 역할을 했다.

## Next steps inferred

- Phase 2: FrameAnalysisService 구현 (Claude structured output → Frame A/B/C/D 스코어)
- `decision_events` 테이블에 `frame_a`, `frame_b`, `frame_c`, `frame_d` 컬럼 추가 마이그레이션
- 대시보드 타임라인에 Frame 배지 표시
- Git hook 설치 스크립트 정식화 (`hooks/install.sh` 완성)
